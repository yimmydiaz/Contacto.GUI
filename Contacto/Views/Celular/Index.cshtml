@model IEnumerable<Contacto.ModelBD.tb_Celular>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.celular)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.principal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_medioContacto.correo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.celular)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.principal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_medioContacto.correo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idcelular }) |
            @Html.ActionLink("Details", "Details", new { id=item.idcelular }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idcelular })
        </td>
    </tr>
}

</table>
