@model IEnumerable<Contacto.ModelBD.tb_contacto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tb_usuario.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_usuario1.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tb_usuario.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_usuario1.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idcontacto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idcontacto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idcontacto })
        </td>
    </tr>
}

</table>
